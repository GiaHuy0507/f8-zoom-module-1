{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "vite build && gh-pages -d dist"
  },
  "dependencies": {
    "gh-pages": "^6.3.0",
    "sass": "^1.102.0",
    "vite": "^8.1.5",
    "vite-plugin-ejs": "^2.0.0"
  }
}
